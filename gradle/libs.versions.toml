[versions]
# Kept in lockstep with swag-pay so the composite build shares one toolchain.
agp = "8.12.1"
kotlin = "2.4.10"
coroutines = "1.11.0"
serialization = "1.9.0"
ktor = "3.6.0"
# Compiled against 4.12 so the interceptor links against both OkHttp 4 and 5 at runtime.
okhttp = "4.12.0"
androidxCore = "1.13.1"
junit = "4.13.2"
# Optional integrations: compileOnly, active only when the app ships them.
mmkv = "2.4.2"
firebaseConfig = "23.1.0"

[libraries]
kotlinx-coroutines-core = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-core", version.ref = "coroutines" }
kotlinx-coroutines-android = { module = "org.jetbrains.kotlinx:kotlinx-coroutines-android", version.ref = "coroutines" }
kotlinx-serialization-json = { module = "org.jetbrains.kotlinx:kotlinx-serialization-json", version.ref = "serialization" }
ktor-server-cio = { module = "io.ktor:ktor-server-cio", version.ref = "ktor" }
ktor-server-test-host = { module = "io.ktor:ktor-server-test-host", version.ref = "ktor" }
okhttp = { module = "com.squareup.okhttp3:okhttp", version.ref = "okhttp" }
okhttp-mockwebserver = { module = "com.squareup.okhttp3:mockwebserver", version.ref = "okhttp" }
androidx-core = { module = "androidx.core:core", version.ref = "androidxCore" }
junit = { module = "junit:junit", version.ref = "junit" }
mmkv = { module = "com.tencent:mmkv", version.ref = "mmkv" }
firebase-config = { module = "com.google.firebase:firebase-config", version.ref = "firebaseConfig" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
android-library = { id = "com.android.library", version.ref = "agp" }
kotlin-jvm = { id = "org.jetbrains.kotlin.jvm", version.ref = "kotlin" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
